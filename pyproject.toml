[project]
name = "email-triage-env"
version = "0.1.0"
description = "Email triage system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "python-dotenv",
    "pandas"
]